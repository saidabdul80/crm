name: Deploy to Production

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Start SSH agent and load private key
        uses: webfactory/ssh-agent@v0.9.0
        with:
          ssh-private-key: ${{ secrets.DEPLOY_KEY }}

      - name: Setup known hosts
        run: ssh-keyscan -H 34.252.126.11 >> ~/.ssh/known_hosts

      - name: Install dependencies and deploy
        run: |
          ssh -o StrictHostKeyChecking=no mf4vad6qmw1k@198.12.235.72 "cd ~/public_html/clients.cowris.com && git pull origin master && php artisan optimize:clear"
